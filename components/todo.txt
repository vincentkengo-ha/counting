Improve the layout of AddRow[x]
Prevent duplicate names[]
Prevent negative points (disable the '-' button when count === 0)[x]
Prevent items with empty names[x]
Clear the TextInput after adding an item[x]
Automatically hide the soft keyboard after adding an item[x]
Keep the list sorted according to names[x]
Show an alternative view when the list is empty (E.g. "No items yet, add something!")[]
Add a possibility to remove items[]
Add a possibility to edit names[]